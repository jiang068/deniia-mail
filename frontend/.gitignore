# ---- 依赖 / 构建产物 ----
node_modules/
dist/
dist-ssr/
*.local

# ---- 开发 / 部署缓存 ----
.vscode/
.idea/
.DS_Store
Thumbs.db
.wrangler/

# ---- 环境变量 / 密钥 ----
.env
.env.*
!.env.example

# ---- 日志 / 临时文件 ----
*.log
npm-debug.log*
*.tsbuildinfo

# ---- 运行时配置（含真实域名/后端 URL，勿提交）----
# 部署前复制 config.json.example 为对应位置的 config.json 并填写。
# public/config.json 会被 Vite 拷入 dist，供 SPA 在 /config.json 运行时读取；
config.json
public/config.json

# ---- 部署配置（含真实账号 id，勿提交）----
wrangler.toml