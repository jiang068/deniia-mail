# ---- 依赖 / 构建产物 ----
node_modules/
dist/
dist-ssr/
*.local

# ---- 开发 / 部署缓存 ----
.vscode/
.idea/
.DS_Store
Thumbs.db
.wrangler/

# ---- 环境变量 / 密钥 ----
.env
.env.*
!.env.example

# ---- 日志 / 临时文件 ----
*.log
npm-debug.log*
*.tsbuildinfo

# 项目根目录的旧 config.json 副本（运行时不使用，勿提交）
config.json
# 但 public/config.json 会被 Vite 拷贝进 dist 并由 SPA 在 /config.json 运行时读取，必须提交
!public/config.json