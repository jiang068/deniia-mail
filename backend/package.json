{
  "name": "deniia-mail",
  "version": "0.1.0",
  "private": true,
  "description": "Serverless Webmail - Cloudflare Workers + Pages",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute mail-db --file=schema.sql",
    "db:init:local": "wrangler d1 execute mail-db --local --file=schema.sql"
  },
  "dependencies": {
    "postal-mime": "^2.5.0"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}